GenFPS is a Android specific tool which can analyze the average FPS value.
It used to deal with the log file which generated by logcat filter by suffaceFlinger. In the terminal:
genFPS -i logfile
the average FPS value will display on the STDOUT.
